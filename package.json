{
  "scripts": {
    "dev": "tsc -w",
    "build": "npm run clean && npm run copy && tsc",
    "clean": "rm -rf dist",
    "copy": "mkdir -p dist && cp -r manifest.json icon dist"
  },
  "devDependencies": {
    "@types/chrome": "^0.0.323",
    "typescript": "^5.8.3"
  }
}
