{
  "scripts": {
    "dev": "tsc -w",
    "build": "npm run clean && npm run copy && tsc",
    "clean": "rm -rf dist",
    "copy": "mkdir -p dist && cp -r manifest.json icon dist",
    "release": "npm run build && cd dist && zip -r ../extension.zip ."
  },
  "devDependencies": {
    "@types/chrome": "^0.1.39",
    "typescript": "^6.0.2"
  }
}
